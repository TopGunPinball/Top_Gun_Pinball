#config_version=6

coils:

# Flippers
  c_flipper_left:
    number: 
    allow_enable: true
    default_pulse_ms: 20        # duration the coil is activated
    default_pulse_power: 0.5    # percentage of coil power during initial pulse (0 - 1)
    default_hold_power: 0.12
  c_flipper_left_upper:
    number: 
    allow_enable: true
    default_pulse_ms: 20        # duration the coil is activated
    default_pulse_power: 0.5    # percentage of coil power during initial pulse (0 - 1)
    default_hold_power: 0.12
  c_flipper_right:
    number: 
    allow_enable: true
    default_pulse_ms: 20        # duration the coil is activated
    default_pulse_power: 0.5    # percentage of coil power during initial pulse (0 - 1)
    default_hold_power: 0.12
  
  # Trough and plunger
  c_trough_eject:
    number:
    default_pulse_ms: 10
  c_plunger:
    number:

# Sling Shots and Pop Bumpers
  c_left_sling:
    number: 
    default_pulse_ms: 15      # duration the coil is activated
    platform_settings:        # minimum time between pulses for THIS coil        
      recycle_ms: 250
    label: "Left Slingshot"
    tags: [sling, playfield]
  c_right_sling:
    number: 
    default_pulse_ms: 15      # duration the coil is activated
    platform_settings:        # minimum time between pulses for THIS coil        
      recycle_ms: 250
    label: "Right Slingshot"
    tags: [sling, playfield]
  c_left_bumper:
    number: 
    default_pulse_ms: 15      # duration the coil is activated
    platform_settings:        # minimum time between pulses for THIS coil        
      recycle_ms: 250
    label: "Left Pop Bumper"
    tags: [pop bumper, playfield]
  c_right_bumper:
    number: 
    default_pulse_ms: 15      # duration the coil is activated
    platform_settings:        # minimum time between pulses for THIS coil        
      recycle_ms: 250
    label: "Right Pop Bumper"
    tags: [pop bumper, playfield]
  c_center_bumper:
    number: <your FAST coil number>
    default_pulse_ms: 15      # duration the coil is activated
    platform_settings:        # minimum time between pulses for THIS coil        
      recycle_ms: 250
    label: "Center Pop Bumper"
    tags: [pop bumper, playfield]

coil_player:

# Auto Shooter, Plunger Lane
  fire_autoplunger:
    c_plunger:
      pulse_ms: 30


