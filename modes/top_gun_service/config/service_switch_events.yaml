#config_version=6
##############################################################################
# TOP GUN SERVICE MODE - SWITCH STATE EVENTS
# This file posts events when switches change state during switch test
# Include in the mode via the mode's config list
##############################################################################

event_player:
  ##############################################
  # SWITCH STATE MONITORING
  # Posts service_switch_activated for any switch hit during switch test
  # Also posts state change events for Godot to update display
  ##############################################
  
  # All switches - post activation event (for sound)
  s_start_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_tilt_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_coin_door_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_coin_door_service_1_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_coin_door_service_2_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_coin_door_service_3_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_coin_door_service_4_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_coin_door_credit_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_coin_door_credit_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_flipper_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_flipper_left_eos_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_flipper_left_upper_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_flipper_left_upper_eos_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_flipper_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_flipper_right_eos_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_trough_1_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_trough_2_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_trough_3_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_trough_4_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_trough_5_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_trough_6_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_jam_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_shooter_lane_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_bumper_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_bumper_center_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_bumper_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_slingshot_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_slingshot_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_outlane_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_inlane_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_inner_inlane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_outer_inlane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_outlane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_mig_lane_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_mig_lane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_lane_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_orbit_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_orbit_upper_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_orbit_lower_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_orbit_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_lane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_i_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_c_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_e_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_m_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_a_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_n_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_l_lock_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_o_lock_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_c_lock_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_k_lock_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_barrier_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_flight_gear_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_spinner_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_ramp_left_exit_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_ramp_right_exit_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_ramp_inner_exit_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_ramp_tower_exit_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_lock_plunger_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_lock_mav_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_lock_goose_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated
  s_mode_start_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    - service_switch_activated

variable_player:
  ##############################################
  # LAST SWITCH TRACKING
  # Sets machine.service_last_switch to the name of the most recently
  # activated switch. Reference in Godot as machine.service_last_switch
  ##############################################
  s_start_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_start"
  s_tilt_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_tilt"
  s_coin_door_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_coin_door"
  s_coin_door_service_1_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_coin_door_service_1"
  s_coin_door_service_2_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_coin_door_service_2"
  s_coin_door_service_3_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_coin_door_service_3"
  s_coin_door_service_4_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_coin_door_service_4"
  s_coin_door_credit_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_coin_door_credit_right"
  s_coin_door_credit_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_coin_door_credit_left"
  s_flipper_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_flipper_left"
  s_flipper_left_eos_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_flipper_left_eos"
  s_flipper_left_upper_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_flipper_left_upper"
  s_flipper_left_upper_eos_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_flipper_left_upper_eos"
  s_flipper_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_flipper_right"
  s_flipper_right_eos_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_flipper_right_eos"
  s_trough_1_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_trough_1"
  s_trough_2_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_trough_2"
  s_trough_3_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_trough_3"
  s_trough_4_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_trough_4"
  s_trough_5_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_trough_5"
  s_trough_6_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_trough_6"
  s_jam_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_jam"
  s_shooter_lane_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_shooter_lane"
  s_bumper_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_bumper_left"
  s_bumper_center_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_bumper_center"
  s_bumper_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_bumper_right"
  s_slingshot_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_slingshot_left"
  s_slingshot_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_slingshot_right"
  s_outlane_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_outlane_left"
  s_inlane_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_inlane_left"
  s_inner_inlane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_inner_inlane_right"
  s_outer_inlane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_outer_inlane_right"
  s_outlane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_outlane_right"
  s_mig_lane_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_mig_lane_left"
  s_mig_lane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_mig_lane_right"
  s_lane_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_lane_left"
  s_orbit_left_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_orbit_left"
  s_orbit_upper_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_orbit_upper"
  s_orbit_lower_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_orbit_lower"
  s_orbit_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_orbit_right"
  s_lane_right_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_lane_right"
  s_i_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_i_iceman_target"
  s_c_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_c_iceman_target"
  s_e_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_e_iceman_target"
  s_m_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_m_iceman_target"
  s_a_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_a_iceman_target"
  s_n_iceman_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_n_iceman_target"
  s_l_lock_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_l_lock_target"
  s_o_lock_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_o_lock_target"
  s_c_lock_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_c_lock_target"
  s_k_lock_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_k_lock_target"
  s_barrier_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_barrier_target"
  s_flight_gear_target_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_flight_gear_target"
  s_spinner_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_spinner"
  s_ramp_left_exit_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_ramp_left_exit"
  s_ramp_right_exit_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_ramp_right_exit"
  s_ramp_inner_exit_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_ramp_inner_exit"
  s_ramp_tower_exit_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_ramp_tower_exit"
  s_lock_plunger_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_lock_plunger"
  s_lock_mav_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_lock_mav"
  s_lock_goose_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_lock_goose"
  s_mode_start_active{mode.top_gun_service.active and machine.service_menu_level==1}:
    service_last_switch:
      action: set_machine
      string: "s_mode_start"